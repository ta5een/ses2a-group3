const Auth = require("./auth.controller");
const Group = require("./group.controller");
const Interest = require("./interest.controller");
const User = require("./user.controller");

module.exports = {
  Auth,
  Group,
  Interest,
  User,
}
